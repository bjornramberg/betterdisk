[package]
name = "betterdisk"
version = "0.1.0"
edition = "2021"

[dependencies]
ratatui = { version = "0.28", features = ["crossterm"] }
crossterm = "0.28"
walkdir = "2.5"
sysinfo = "0.33"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true